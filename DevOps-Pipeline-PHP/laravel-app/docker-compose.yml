version: "3.9"

services:
  app:
    image: abdo1997mohamed2030/comingsoon-page:latest
    container_name: comingsoon-page
    ports:
      - "80:80"
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
    volumes:
      - .:/var/www/html